# PixShell 原生化 · 代码概要（接手指南）

> 目的：即使本会话中断/记录丢失，凭此文档也能完整接手。涵盖 mac + Windows 两个原生项目。
> 规则：**每改一次代码都要同步更新本文档**（用户明确要求）。
> 最后更新（第 3 批）：新增 **自定义命令历史悬浮菜单（支持单条运行/复制/删除及全量清空）**、**快速连接面板毛玻璃背景（防重叠）**、
> 默认启动窗口比例调优为 800x600 (4:3) 强制生效、修复标签页关闭按钮点击区域过小的问题、全局默认主题改为水墨(ink)、**优化快捷命令分组按钮图标与文字间距（移除多余空格）**、
> **合并快捷命令板「发送到」操作区**（去除列表侧冗余发送栏，统一由右侧编辑器下方接管，释放预设命令显示空间）、
> **修复连接管理器分组与列表无法滚动导致重叠的布局缺陷**（解除了 DocumentView 对 ScrollView TopAnchor 的错误约束）。
> 历史更新（第 2 批）：新增 **文件日志**、**菜单栏+快捷键**、**内置文本编辑器**、**结构化系统信息**、**32 套终端配色(含设置页选择)**、
> **命令框历史/Tab 补全/${参数}/cd↔SFTP 同步**、**快捷命令分组板**、**SFTP 多选+右键菜单+拖拽上传+智能打包传输**、
> **底栏可拖拽调高(向上拖变高，最小 200)**、**标签右键菜单/同主机多开**、**会话输出缓冲(500KB)**、
> **私钥认证(ed25519/P256/P384/P521，密钥优先→密码兜底)**、**终端右键菜单+12 个背景预设**、**连接管理器分组增删改**、
> **代理服务器(SOCKS5/SOCKS4/HTTP 手写 NIO 握手 + 管理面板 + 按主机 proxyId 生效)**、**工具面板进程/网络结构化表格 + 结束进程**。Windows 端已完成布局同步（0 error），特性二次同步进行中。
> 上一批：对齐老仓库源码（`/Volumes/d/pixshell`，**必须看源码不要猜**）重做 chrome 与顶栏两个图标——
> 侧栏可整块收起为 26pt 窄轨(⟩侧栏)、文件/命令坞整块可收、文件操作图标与 文件/命令 同行、命令栏在坞上方；
> 宫格=工具面板(#toolsPanel)、汉堡=嵌套主菜单(#mainMenu)；新增备份选项面板。均 GUI 截图/像素采样验证。
> Windows 端尚为旧版布局（未同步本轮大改）。


## 语义高亮：规则顺序就是优先级（改之前先读这段）

`Highlight/SemanticHighlight.{swift,cs}` 用**私有区占位符**实现"先匹配者胜"：命中的片段先换成
`SO<index>EO`，后续规则跳过占位区。所以**规则的书写顺序 = 优先级**，把泛规则写在具体规则前面
一定出错。这不是理论 —— 实跑测出过两个：

| 输入 | 错误表现 | 原因 | 修法 |
|---|---|---|---|
| `2026-07-26 12:30:00` | `12:30:00` 染成 **IP 青色** | 裸 IPv6 规则排在日期规则前面，两个冒号 + hex 字符全中 | 日期三条规则提到 IP 之前 |
| `--> src/main.rs:12:9` | `:12` 染成 **端口绿色** | 通用 `:PORT` 排在 Rust 规则前面 | Rust 工具链整块提到最前 |

现行顺序（别随便动）：URL → `***`警告块 → **Rust 工具链块** → 权限位 → 路径 →
**时间戳** → MAC → IPv4 → `[IPv6]:port` → 裸 IPv6 → user@host → 域名 → `*:PORT` → `:PORT` →
大小/速率 → **UUID**(必须先于 hash) → hex → hash → HTTP 状态码 → 错误/警告 → 成功词 →
百分比 → HTTP方法/进程名[pid]/时长/key= → 运维关键词 → 引号字符串 → 括号。

**借鉴来源**：类别清单参考了 [tailspin](https://github.com/bensadeh/tailspin)，补进了
UUID / HTTP 方法 / `进程名[pid]` / 时长 / `key=value` 的 key / 引号字符串六类。
**没有引入 tailspin 本身**：它是 Rust crate 且**按行处理**，而我们喂的是活的 PTY 流
（半行、光标控制、`top`/`vim` 全屏重绘），按行高亮会把交互式程序的画面搞坏。

### 回归测试（Windows，`C:\hltest`）
独立控制台工程，只编译 `SemanticHighlight.cs` + 一个 6 行的 `HighlightColors` 替身
（真实实现依赖 WPF）。刻意放在**仓库外** —— SDK 风格工程默认 glob `**/*.cs`，放进仓库会被
主工程一并编译进去。
```
ssh pixwin "cd /d C:\hltest && dotnet run --project hl.csproj"
```
两类断言：
1. **不变式**：`Strip(Decorate(x)) == Strip(x)` —— 脱掉 SGR 后必须与脱色原文逐字相同，
   既不能吞字也不能多字。注意两边**都要脱色**：原文本身可能带转义，只脱一边就是两把尺子
   （这个坑让测试误报过一次"高亮吃字"）。
2. **针对性回归**：上表那两条，防止顺序被改回去。

当前状态：不变式 0 失败，回归全过。

## Windows 构建脚本 `build.bat` / `构建.bat`

双击即可构建（`build.bat run` 构建完顺带启动）。三个已修的坑：

1. **"没找到 dotnet"其实是 PATH 没生效**。装完 .NET 没重开窗口，双击 `.bat` 继承的是资源管理器
   启动时的旧环境。所以不能只靠 `where dotnet`，找不到就去固定位置捞
   （`%ProgramFiles%` / `%ProgramW6432%` / `%ProgramFiles(x86)%` / `%LOCALAPPDATA%\Microsoft\dotnet`
   / `%USERPROFILE%\.dotnet`），还可以用环境变量 `PIXSHELL_DOTNET` 强制指定。
2. **`%ProgramFiles(x86)%` 不能放进 `for %%p in (...)` 括号列表** —— 它展开后自带一个 `)`，
   会被 cmd 当成列表收尾括号把语法拆散，残片被当命令执行（现场表现是莫名冒出
   `AT command has been deprecated` / `Invalid command`）。改用 `:probe` 子程序逐个探测。
3. 构建输出**全程 tee 到 `build.log`**，失败时自动 `findstr` 回显报错行 ——
   之前失败只显示"退出码 1"，什么线索都没有。
   另外构建前先 `taskkill PixShell.exe`：否则 MSB3027/CS2012 文件占用必失败
   （**注意** dotnet 的常驻 build server 也会持锁，光杀 PixShell.exe 有时不够，还要杀 dotnet.exe）。


## 0.1 两端同步状态速查（2026-07-27 收盘）

| 能力 | mac | Windows |
|---|---|---|
| 语义高亮（含 cargo/tailspin 类别、规则顺序修正） | ✅ | ✅ |
| 四主题（深/浅/水墨/复古）+ lightKind 语义 | ✅ | ✅ |
| 密钥管理 | ✅ 已验证 | ⚠️ 编译通过，**未在真机点过「生成密钥」** |
| MCP server / pixshell CLI | ✅ | ✅ |
| agent 对话面板 + 机器人按钮 | ✅ | ✅ |
| **命令面板左右分栏重做** | ✅ | ❌ 未同步 |
| **文本编辑器独立窗口 / 保存回报 / 光标修正** | ✅ | ❌ 未同步 |
| `ScrollableText` 工厂（NSTextView 坑） | ✅ | — （WPF 无此问题） |

> 下次接手先看 §7.96，那里记了本轮所有契约变更和三条操作事故。
>
> **分工（2026-07-27）**：Windows 端由**另一个主分支会话**负责同步，本会话不再写
> `/private/tmp/win146/PixShell-win/`（避免互相覆盖）。上表标 ❌ 的两块就是待同步项。

## 0. 背景 / 目标
把原 Electron 版 PixShell（老仓库 `/Volumes/d/pixshell`，SSH/SFTP 客户端）**重写为两个纯原生桌面应用**（路线 A）：
- **mac** → `/Volumes/ssd1t/opencode/PixShell-mac/`（Swift + AppKit + SwiftTerm + swift-nio-ssh）。**我（Claude）直接编写并 GUI 截图验证**。
- **Windows** → `C:\PixShell-win`（= mac 本地 SMB 挂载 `/private/tmp/win146/PixShell-win/`）（C# + WPF + WebView2/xterm.js + SSH.NET）。**由后台 general-purpose 子智能体编写，我审核 + 独立复核 build**。
前端布局**照搬老仓库**（见 `docs/LAYOUT-PARITY.md`）。

## 1. 工具链 / 构建（关键，先看这条）
### mac
- **必须** `export DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer`（xcode-select 指向的 CLT 版 SwiftPM 有缺陷，裸 `swift build` 会链接失败：`PackageDescription.Package.__allocating_init undefined for x86_64`）。完整 Xcode 26.3 在 /Applications。机器是 Intel x86_64。
- 构建：`export DEVELOPER_DIR=... && swift build`；运行 `.build/debug/PixShell`；打包 `bash scripts/package-mac.sh debug` → `dist/PixShell.app`（ad-hoc 签名，可双击）。
- Package：`swift-tools-version: 6.0`，`swiftLanguageVersions: [.v5]`。依赖：SwiftTerm(1.2+)、apple/swift-nio-ssh(0.9+)、apple/swift-nio(2.65+)。
### Windows
- 远端构建走**稳定连接**：`ssh pixwin "cd /d C:\PixShell-win && dotnet build"`（见 §5）。
- .NET SDK 9.0.316 已装；无 VS，命令行 `dotnet build`。WPF（`net9.0-windows`, `UseWPF`）。
- NuGet：Microsoft.Web.WebView2、SSH.NET(2024.2.0)、System.Security.Cryptography.ProtectedData。

## 2. 布局契约（mac 已像素级复刻老仓库；窗口 4:3 竖向 980×1240）
**权威来源：老仓库 `packages/app/renderer/index.html` + `app.js` + `代码说明概要.md` §7.1/§7.2/§7.7 —— 改 UI 前先读它，不要照截图猜。**
```
顶栏 top-bar: [红绿灯留位][侧栏折叠][display=连接管理器][plus.app=新建] [会话胶囊tab…] [＋快连]  [月亮/太阳=主题][宫格=工具面板][汉堡=主菜单]
侧栏 sidebar = 服务器监控仪表盘（非主机列表）：已连接状态/IP复制/系统信息 · 实时监控(运行/负载/CPU/内存/交换 药丸条) · 进程TOP · 网络(iface+火花线) · 延迟/网关 · 磁盘
        折叠 → 收成 26pt 窄轨 + 「⟩侧栏」竖条（点竖条展开）；分隔条可拖宽，拖到 <90 自动收起
工作区 work-center（自上而下）：
  · 内容区：无活动会话 → QuickConnect 落地页；有会话 → SwiftTerm 终端
  · 命令栏 cmd-bar（命令 输入 历史 发送 ▾）—— **在坞的上方**（老仓库 §7.1「命令栏在底栏上方」）
  · 底部坞 bottom-dock：单行 [文件][命令] + 文件操作图标(‹ 路径 ⟳ ⬇ ⬆ 📁+ 🗑 ▯，与 tab 同一行) / 面板体
    命令栏右侧 ▾/▴ 隐藏/显示**整个坞**（高度→0，非只隐藏内容）
状态栏 statusbar: PixShell 0.1.0 · ●CLI · 就绪 | UTF-8 · 已连接 <host>
```
**状态栏 CLI 三态（严格对齐老仓库口径，别把「桥在监听」写成「已连接」）**：未开启(红)=桥没在听 / 已开启(黄)=桥在听但无外部请求 / 已对接(绿)=5 分钟内有鉴权通过的外部请求。状态由 `AppDelegate.bridgeStatus` 驱动（与桥实现解耦）。
**主题持久化**：`Theme.dark` 优先级 = `PIXSHELL_THEME` 环境变量 > UserDefaults `pixshell.theme` > 默认深色；切换即保存，启动日志会打印实际生效值。
**交互补齐（老仓库 docs/ui/interaction-logic.md）**：连接管理器主机行**右键**=连接/新开会话/编辑/复制主机/**连接本组**/删除；焦点模型 `⌃;` 聚焦命令框、`` ⌃` `` 聚焦终端、命令框内 `Esc` 回终端。
**chrome 收起规则（对齐老仓库 `applyChromeForTab`）**：快速连接/落地页 → 自动收起侧栏+坞；SSH 连上/切到已连接会话 → 自动展开两者。
**顶栏两个图标的真实职责（老仓库 §7.2，曾接错）**：
- **宫格 = 工具面板**（`#toolsPanel`）：主机下拉 + 路由追踪/进程管理/网络监控/速度测试/自定义加速 + 下载目录选择/打开 + 输出框。**不是快速连接。**
- **汉堡 = 主菜单**（`#mainMenu`，二级嵌套）：文件 / 查看 / 选项 + 密钥管理器 + 云端同步(备份选项…) + 帮助。
主机列表在**连接管理器弹窗**里（不在侧栏）。详见 `docs/LAYOUT-PARITY.md`。
**配色令牌从老仓库 shell.css `:root` 1:1 提取（见 `UI/Theme.swift`，明暗两套 computed）**：
- 深色：bg `#1c1c1e` / bg2 `#2c2c2e` / bg3 `#3a3a3c` / term `#191c27` / border `rgba(255,255,255,.10)` / text `#f7f7fa` / accent `#0a84ff` / ok `#30d158` / warn `#ffd60a` / err `#ff453a`。
- 浅色：bg `#f2f2f7` / bg2 `#ffffff` / term `#d4d6dc` / text `#1c1c1e`（accent/ok/warn/err 两主题通用）。
- 圆角 radius 10 / sm 7 / lg 14。
- **分隔条**：侧栏/底栏分隔线视觉只有 **1pt**（不占 UI 空间），但 `DividerView`/`HDividerView` 把**命中区各外扩 3pt**（重写 `hitTest` + `resetCursorRects`），细线也好拖。

## 3. mac 项目逐文件（`Sources/PixShell/`，60 文件 / ~13500 行）
- `main.swift` — AppDelegate 类声明 + 状态字段（含 quickConnect/monitor/connMgr/sysInfo/workSplit/dockCollapsed…）+ 生命周期 + 入口。`darkTheme` 现为 computed = `Theme.dark`（主题可运行时切）。**模块化**：其余按职责拆成扩展。
- `App/AppDelegate+Layout.swift` — 五区布局。`installContent()`（可整窗重建，供主题切换；重建后把当前会话终端挂回并按需显落地页）；顶栏/侧栏(=MonitorSidebar)/工作区/命令栏/底部坞/状态栏；连接管理器+系统信息弹层；`showQuickConnect()`；`toggleTheme→applyTheme`（整窗重绘）；`toggleDock`（折叠 文件/命令，改 workSplit 分割位）；字号±/高亮开关/汉堡菜单。命令栏现在**下部最底**、文件命令坞可折叠。
- `App/AppDelegate+TermMenu.swift` — 终端右键菜单：复制/粘贴/全选/清屏 + **设置背景**(12 个预设色板，同老仓库 TERM_BG_PRESETS，带色块预览、勾选当前、恢复默认) + 字号±。覆盖色存 `pixshell.termBgOverride`。
- `App/AppDelegate+MainMenu.swift` — **系统菜单栏 + 全局快捷键**（应用/文件/编辑/查看/会话/帮助）：⌘I 系统信息、⌘J 工具、⌘L 连接管理器、⌘B 底栏、⌘⇧S 侧栏、⌘K 清屏、⌘{ ⌘} 切标签、⌃1..9 跳标签、⌘W 关标签。
- `App/AppDelegate+CommandBox.swift` — 命令框交互：↑↓ 历史、Tab 远端路径补全（ls 候选+公共前缀）、`${参数}` 弹框、发送后保持焦点、`cd` → SFTP 目录同步、SFTP 进目录 → 反向 `cd` 终端、**历史弹出悬浮菜单(`CommandHistoryVC`)**。
- `App/AppDelegate+Menu.swift` — **汉堡菜单各项实现**：连接/断开/重连、导入/导出主机 JSON、进程管理/网络监控（复用工具面板执行）、复制/粘贴/清屏（走 SwiftTerm 自带 `copy(_:)`/`paste(_:)`）、备份面板、设置弹窗（主题/字号/高亮）、关于/仓库。
- `App/AppDelegate+Hosts.swift` — 主机增改删（写 Keychain）+ `refreshHostUI`（刷新 connMgr + quickConnect）。
- `App/AppDelegate+Sessions.swift` — 会话开/切/关；`openSession`(解析密码：env→Keychain→**弹框输入**)、`beginSession`(真正连+记 recents)、`promptPassword`(sheet+记住勾选)；SSHSessionDelegate（`connected` 标志、认证失败自愈清 Keychain）；**didReceive 语义高亮：染色后仍走 `termView.feed(byteArray:)`**（关键，见 §6）；监控采集定时器 `monitorCommand`（/proc 一行命令）+ `parseMonitor`。
- `Session/TermSession.swift` — 会话模型（+`connected` 实时连接标志）。
- `Model/Host.swift` — 主机模型 id/name/host/port/username/group/osId/**keyPath(私钥)**（无密码；`init(from:)` 用 decodeIfPresent，老 hosts.json 仍可解码）。
- `Store/Log.swift` — **文件日志**（对齐老仓库 logger.js）：`~/Library/Application Support/PixShell/logs/` 下 `pixshell-YYYY-MM-DD.log`（按天）+ `pixshell-runtime.log`（滚动保留最近 1000 行）。`Log.info/warn/error/debug(msg, tag)`，异步串行写、**绝不抛异常**。已埋点：app 启动、ssh 连接/握手/关闭、会话打开、认证失败清密码、UI 动作（系统信息/工具/主题/侧栏/底栏折叠）、编辑器打开/保存。**排查问题先看这个文件。**
- `Bridge/AgentBridge.swift` + `Bridge/BridgeRoutes.swift` — **本地 CLI/AI-Agent 桥**（**坑**：accept 到的 NWConnection 必须被**持有**，否则 ARC 立刻回收、连接在 receive 前就被拆掉，表现为「端口在听但请求永远无响应」；另需在请求分发后停止继续 receive，否则尾随字节会触发重复分发，例如 exec 执行两次）（对齐老仓库 agent-bridge.js）：仅监听 `127.0.0.1:8766`（`PIXSHELL_BRIDGE_PORT` 可改），强制 token 鉴权（`~/Library/Application Support/PixShell/agent_token`，0600，32 字节随机 hex；支持 `Authorization: Bearer` / `X-PixShell-Token` / `X-Agent-Token` / `?token=`），拒绝带 `Origin` 的请求（防浏览器 CSRF），body 上限 8MB。路由与老仓库同名：`/v1/health`、`/v1/app/hosts|connect|sessions|shell|exec|screen|sftp/{list,download,upload}`。
- `App/AppDelegate+Bridge.swift` — 桥的宿主实现（`BridgeHost`）：主机/会话列表（**绝不含密码**）、按 hostId 连接（只用已保存凭据，不弹框）、写 shell、一次性 exec、读最近输出、SFTP 列表/上传/下载。
- `Store/AppUpdate.swift` — 软件更新检查（对齐老仓库 app-update.js 判定）：GitHub Releases API 取 latest → 语义化版本比较 → 「发现新版本 / 已是最新 / 无法获取」。**只检查+开发行页，不自动下载安装**（原生包要签名公证，自动替换风险高）。菜单：云端同步 → 软件更新。
- `Store/BackupBundle.swift` — **备份包 v1**（格式对齐老仓库 sync/index.js）：`{version, exportedAt, hosts(不含密码), settings, quickCommands}`；`WebDAVBackup` 用 Basic 认证 PUT/GET 真实上传下载（菜单 云端同步 → WebDAV 设置/上传/恢复）。导入兼容旧的纯 `[Host]` 数组。
- `Store/CommandBox.swift` — 命令框纯逻辑（移植老仓库 sync.js/history.js/params.js）：`CommandSync`(normalize/join/applyCd)、`CommandHistory`(push/older/newer/filter + 持久化)、`CommandParams`(parse/render/hasUnresolved)。
- `Store/QuickCommands.swift` — 快捷命令模型 + 分组/渲染 + 持久化 `quick-commands.json`，默认集同老仓库 DEMO_QUICK_COMMANDS；`SendTarget`(当前/所有已连接/指定会话)。
- `Store/HostStore.swift` — 主机 JSON `hosts.json` + **recents 历史** `recents.json`（`recentHosts`/`noteRecent`/`clearRecents`，首运行以已存主机播种）。
- `Store/Keychain.swift` — 密码存 Keychain（按 host id）+ `has(id)`（只探测存在、不解密、不弹授权，供徽章）。
- `SSH/SSHSession.swift` — SSH 会话协议 + `SSHCredentials` + `exec`。
- `SSH/NIOSSHSession.swift` — swift-nio-ssh 交互式 PTY shell（连接/认证/PTY/shell/收发/resize/close）+ `exec`(ExecCollectHandler)。**`SSHUserAuthDelegate`：私钥优先(ed25519/P256/P384/P521，OpenSSH v1 + PKCS8 PEM；RSA/加密/SEC1 记日志跳过)，再回退密码；两种方式各只提交一次**（被拒即干净失败，避免反复重试触发服务器锁定）。
- `SFTP/SFTPService.swift` — SFTP 服务协议 + `SFTPEntry`(name/isDir/size/mtime/perms)/`SFTPError`。
- `SFTP/SFTPProtocol.swift` — SFTP v3 线协议编解码（ByteBuffer 扩展）。
- `Proxy/ProxyConfig.swift` — 代理模型 + `ProxyStore`（`proxies.json`）：类型 socks5/socks4/http（兼容老仓库数字码 100/200），默认端口 1080/8080。`ssh-jump` 仅保留兼容解析，拨号时记日志并直连。
- `Proxy/ProxyHandlers.swift` — **手写 NIO 握手**（零新依赖）：SOCKS5(含 RFC1929 用户名密码 + DOMAINNAME 由代理侧解析 DNS)、SOCKS4a、HTTP CONNECT(含 Basic 认证)。握手成功后自摘除、再接 SSH 握手；失败给明确错误并关闭。**坑：握手必须从 `handlerAdded`（且 `channel.isActive` 时）启动，不能只依赖 `channelActive`** —— 实际拨号路径里 handler 是在 channel 已 active 之后才加入的，写在 channelActive 会永远不触发、每条代理连接都挂到超时（已用 loopback 假代理服务器实测覆盖：分段回包、认证失败、拒绝连接、超时）。
- `UI/ProxyPanel.swift` — 代理管理弹窗（新建/编辑/删除，名称/类型/主机/端口/用户名/密码）。菜单：选项 → 代理服务器…
- `SFTP/SFTPTransfer.swift` — **智能打包传输**（移植老仓库 native-102）：阈值 8MB；`shouldPack`(多项/目录/大文件)、`packCommand`(远端 `tar -czf` 逐项 `-C 父目录 base`，避免绝对路径进包)、`packLocal`/`extractLocal`(本地 tar)、`quote`(shell 转义)。下载=远端打包→下载→本地解压→两端清理；上传=本地打包→上传→远端解压+删包。拖拽上传也走这条路径（可拖目录）。
- `SFTP/NIOSFTPSession.swift` — **自研 SFTP v3**（over swift-nio-ssh，零新依赖）：list/download/upload/mkdir/remove/rename/realpath/home。
- `Highlight/SemanticHighlight.swift` — 语义高亮引擎（移植老仓库 21 条规则 + HL_LIGHT/DARK truecolor）。**已接实时流并生效**。
- `UI/Theme.swift` — 设计令牌（shell.css 1:1，明暗 computed 色 + 圆角/尺寸 + `ui()/mono()` 字体 + `NSView.rounded`）。
- `UI/Components.swift` — 自绘组件：FlippedView / **DividerView**(左右调整光标) / **HeaderPanGesture**(弹窗头部拖动，**落在 NSControl 上不识别** —— 否则会吞掉「关闭」按钮点击，曾导致系统信息弹窗关不掉) / PillButton / Badge / CardView / IconButton / Dot。
- `UI/ToolsPanel.swift` — **工具面板（宫格图标）**：主机下拉 + 5 chips + 下载目录 + 输出区。输出区两态：**纯文本**（路由追踪/测速）或**结构化表格**——「进程管理」出 PID/用户/内存/CPU/名称|命令行 表 + **结束进程(SIGTERM，确认后执行并自动刷新)**；「网络监控」出 协议/状态/监听地址/端口/进程 表（对齐老仓库 fillProcessTable / fillNetworkTable）。远端命令与老仓库 ssh-engine 一致：`ps -eo pid,user,rss,pcpu,comm,args --sort=-pcpu`、`ss -tulnpH || netstat -tulnp`、`ping -c4 + traceroute/tracepath`、curl 测速。
- `UI/EditorPanel.swift` + `UI/EditorSyntax.swift` — **内置文本编辑器**：卡片弹窗（可拖/点遮罩/Esc 关，脏数据二次确认）+ NSRulerView 行号栏(32pt) + 查找/上一个/下一个/替换/全部替换 + 行号·自动换行开关 + 页脚(行:列/字符数/UTF-8)。`EditorSyntax.detect(path:)` 按扩展名判语言（含 Dockerfile/Makefile），`highlight(_:lang:dark:)` 明暗两套配色；**>180KB 跳过高亮**；只加属性不改字符，保证文本不被吞。入口：SFTP 双击文件 / 右键「打开」→ 下载到临时文件 → 编辑 → 保存回写远端。
- `Monitor/SysInfoParser.swift` — 系统信息 `KEY=value` 解析（纯逻辑、无 AppKit）：`SysInfo{hostname,distro,kernel,arch,uptime,load,ip,cpu,mem*,swap*,net[],disks[]}`，缺字段不崩。
- `Terminal/TermSchemes.swift` — **32 套终端配色方案**（从老仓库 schemes.js 全量移植，含 34 条别名如 `monokai→monokai_soda`）：`TermScheme{id,name,background,foreground,cursor,selection,ansi[16],isDark}`、`TermSchemes.all/get(_:)/defaultDarkId/defaultLightId`。
- `UI/BackupPanel.swift` — **备份选项**（菜单 云端同步）：7 个 provider 卡（本地/WebDAV/GitHub/谷歌云盘/OneDrive/百度/夸克）+ 启用勾选 + 配置 + 导出/导入本地包 + 保存配置。
- `UI/MonitorSidebar.swift` — 服务器监控仪表盘侧栏（.lm-* 复刻：药丸渐变条+条内%、进程表、磁盘表、火花线）。`update(_:)` 解析指标；`setConnected`；回调 onCopyIP/onSysInfo。
- `UI/QuickConnect.swift` — **快速连接/历史落地页**：标题「快速连接（历史·N）」+ ＋新建/清空历史；FlowGrid 主机卡片（图标/名称/root@ip/系统徽章/端口·已记住密码·#序号/连接·编辑）。
- `UI/ConnManager.swift` — 连接管理器弹窗（遮罩+圆角卡片，可拖；分组行+主机行 连接/编辑/删除）。
- `UI/SysInfoPanel.swift` — 系统信息弹窗（exec 采集命令→文本展示，可拖）。
- `UI/TerminalTheme.swift`（TermTheme）— 终端配色：选了方案就用 `TermSchemes`（32 套），否则回退内置明/暗 ANSI 16 色；`schemeId`/`fontSize` 持久化在 UserDefaults，设置弹窗可选。
- `UI/SFTPPanel.swift` — **SFTP 文件浏览器（树+图标明细）**，另含：**⌘多选/Shift 连选**、**右键菜单**(打开/下载/上传/新建目录/重命名/删除/复制路径/插入命令框/刷新)、**拖文件到面板上传**、**F5 刷新 / F2 重命名 / Delete 删除**、双击文件 → 编辑器、`navigate(to:)`/`currentRemotePath`（命令框 cd 同步用）、`saveRemoteFile` 回写：工具条(← 路径 刷新/下载/上传/新建目录/删除/隐藏本地) + 左**远端目录树**(NSOutlineView 懒加载, 黄文件夹图标) + 右**明细列表**(文件名/大小/类型/修改时间, view-based 图标单元格) + 本地列(默认隐藏, 可显)。SFTP 连接/传输逻辑不变。
- `UI/CommandHistoryVC.swift` — **命令历史悬浮菜单（NSPopover）**：完全取代原生 NSMenu，提供滚动列表；悬停显示对应记录的 **运行 / 复制 / 删除** 功能按钮；底部带清空历史功能。
- `UI/CommandPanel.swift` — **快捷命令板**（对齐老仓库 native-94）：目标下拉(当前/所有已连接/指定会话) + 分组 chips + 命令 chips（点发送，`${参数}` 逐个弹框；右键 编辑/删除）+ ＋新建 + 可收起的多行命令编辑器 + **发送条钉底**（发送选中/发送编辑器）。
- `UI/HostEditor.swift` — 主机新建/编辑表单（NSAlert accessory）。
- `scripts/package-mac.sh` — 打 .app 包。


## 3.5 老仓库 23 项功能矩阵对照（迁移完成度自查表）
| # | 老仓库特性 | mac | Windows | 说明 |
|---|-----------|-----|---------|------|
| 1 | 多平台 | ✅ | ✅ | |
| 2 | 多标签 / 批量主机管理 | ✅ | ✅ | 标签右键菜单 + 连接本组 |
| 3 | SSH / RDP | ✅ / ✅ | ✅ / ✅ | RDP 已迁：主机 connectionType=200 → 连接时拉起系统远程桌面（mac `rdp://` via NSWorkspace；win `mstsc /v:host:port`），端口默认 3389；主机表单加「类型」下拉。**更正：老仓库 RDP 并非空壳，app.js 的 connect 分支 `connectionType===200` 是活跃 UI 路径。** |
| 4 | 平滑字体 + 多配色 | ✅ | ✅ | 32 套方案 + 字号 + 设置页 |
| 5 | 终端 SFTP 同屏 + 目录同步 | ✅ | ✅ | cd 双向同步 |
| 6 | 命令自动提示 | ✅ | ✅ | Tab 远端路径补全 |
| 7 | SFTP 优化加载 | ✅ | ✅ | 目录树懒加载 |
| 8 | 网络/性能实时监控 | ✅ | ✅ | 侧栏仪表盘 |
| 9 | 海外加速 | ✅ | ✅ | 同老仓库：主机编辑内配置 |
| 10 | 内存/CPU/Ping/Trace | ✅ | ✅ | 工具面板 |
| 11 | 实时硬盘监控 | ✅ | ✅ | |
| 12 | 进程管理器 | ✅ | ✅ | 进程表 + 结束进程(SIGTERM) |
| 13 | 快捷命令面板 | ✅ | ✅ | 分组 + ${参数} + 发送目标 |
| 14 | 内置文本编辑器 | ✅ | ✅ | 查找替换/行号/高亮/保存回写 |
| 15 | 代理服务器 | ✅ | ✅ | mac 手写 NIO 握手；win 用 SSH.NET 内置 |
| 16 | 打包传输 | ✅ | ✅ | tar 智能打包（≥8MB/多项/目录） |
| 17 | rz/sz zmodem | ➖ | ➖ | 老仓库有模块但 renderer 引用数 0，**不迁** |
| 18 | 多地点 ping | ➖ | ➖ | 老仓库未实现 |
| 19 | 命令框补全/历史 | ✅ | ✅ | |
| 20 | 自定义命令参数 | ✅ | ✅ | |
| 21 | 终端背景 | ✅ | ✅ | 12 预设色；背景图在老仓库 0 引用 |
| 22 | 一键系统信息 | ✅ | ✅ | 结构化卡片 + 表格 |
| 23 | 命令框快选主机文件 | ✅ | ✅ | 右键「插入命令框」 |
| + | 文件日志 / CLI 桥 / WebDAV 备份 / 快捷命令持久化 | ✅ | ✅ | |
| + | 菜单栏快捷键 / 软件更新 / 主题持久化 | ✅ | 部分 | Windows 无 mac 式菜单栏；更新检查未接 |

## 4. Windows 项目逐文件（`C:\PixShell-win`，45 文件 / ~9800 行）
**与 mac 的对应关系**（同名同职责，便于双端对照改）：
- `Logging/Log.cs` ↔ mac `Store/Log.swift`（按天 + runtime 1000 行滚动，异步串行，不抛异常）
- `Monitor/SysInfoParser.cs` + `UI/SysInfoWindow.xaml(.cs)` ↔ mac `Monitor/SysInfoParser.swift` + `UI/SysInfoPanel.swift`（采集命令与 mac **逐字节一致**）
- `Terminal/TermSchemes.cs` + `TermSchemeStore.cs` ↔ mac `Terminal/TermSchemes.swift`（32 套 + 36 别名，脚本比对 0 差异；经 `term.options.theme` 实时应用）
- `UI/EditorWindow.xaml(.cs)` + `UI/EditorSyntax.cs` ↔ mac `UI/EditorPanel.swift` + `UI/EditorSyntax.swift`（20 条高亮正则逐字符比对一致）
- `Store/CommandBox.cs` ↔ mac `Store/CommandBox.swift`（CommandSync/CommandHistory/CommandParams）
- `Theme/*.xaml` + `ThemeManager.cs` ↔ mac `UI/Theme.swift`；`UI/MonitorSidebar/QuickConnectView/ToolsPanel/ConnectionManagerOverlay/MetricBar/Sparkline` ↔ mac 同名面板
- `Sftp/SftpTransfer.cs` ↔ mac `SFTP/SFTPTransfer.swift`（智能打包：阈值 8MB、远端 tar 命令与 mac 逐字节一致、本地用 Win10 自带 bsdtar `tar.exe`）
- `Proxy/ProxyConfig.cs`（含 `ProxyStore`/JSON 转换器）+ `UI/ProxyWindow`/`ProxyEditWindow` ↔ mac `Proxy/ProxyConfig.swift` + `UI/ProxyPanel.swift`
  —— **注意：Windows 不需要 mac 那份手写的 `ProxyHandlers.swift`**，SSH.NET 内置 `ConnectionInfo(ProxyTypes,...)` 直接支持 socks4/5/http。
- `Terminal/TermBackgroundStore.cs` ↔ mac `pixshell.termBgOverride`；终端右键菜单在 `TerminalSession.OnContextMenuRequested`（关掉 WebView2 默认菜单自绘）
- `Store/BackupBundle.cs`（含 `WebDavBackup`）↔ mac `Store/BackupBundle.swift`
- 私钥认证：`HostEntry.KeyPath` + `TerminalSession.BuildConnectionInfo`（SSH.NET `PrivateKeyAuthenticationMethod` → `PasswordAuthenticationMethod` 各一次，不重试以免锁定）
- `Store/QuickCommands.cs` ↔ mac `Store/QuickCommands.swift`（持久化 `%APPDATA%\PixShell\quick-commands.json`，默认集 id 与 mac 逐一致：qc1/2/3/7/4/5/6）
- `Bridge/AgentBridge.cs` + `Bridge/BridgeRoutes.cs` + `MainWindow.Bridge.cs` ↔ mac `Bridge/*` + `App/AppDelegate+Bridge.swift`
  （`HttpListener` 只绑 `http://127.0.0.1:8766/`、逐请求校验 `IPAddress.IsLoopback`、带 `Origin` 一律 403、body 上限 8MiB→413、
   token 存 `%APPDATA%\PixShell\agent_token` 并收紧 ACL、`CryptographicOperations.FixedTimeEquals` 常量时间比较、
   主机列表只回元数据不含密码/私钥、连接只用已保存凭据不弹框；状态栏三态同 mac）
**Windows 窗口 chrome（易踩坑，务必注意）**：Windows 的窗口按钮在**右侧**、顺序是 **最小化 — / 最大化 □ / 关闭 ✕**，
与 mac 左侧红绿灯**方向相反**，不能照搬 mac 布局。实现：`WindowStyle="None"` + `WindowChrome(CaptionHeight=42)` 去掉系统标题栏
（否则会出现"系统标题栏 + 自绘顶栏"双层），顶栏最右自绘三个 `Segoe MDL2` 图标按钮，关闭键悬停 `#E81123` 红底白叉（Windows 习惯）；
顶栏内所有可点控件都要加 `WindowChrome.IsHitTestVisibleInChrome="True"`，否则会被拖拽区吞掉点击；
`StateChanged` 里同步最大化/还原图标（用户可能用 Win+↑ 或双击顶栏）。
另：无边框窗口最大化会溢出显示器工作区（盖住任务栏、顶栏被裁到屏幕外），必须在
`MainWindow.Chrome.cs` 里挂 `WM_GETMINMAXINFO`，把最大化尺寸/位置钳制到当前显示器工作区
（`MonitorFromWindow`+`GetMonitorInfo`，用 rcWork）。这是 Windows 自绘标题栏的必修课，mac 原生无此问题。
**双端功能已对齐**（差异仅在实现手段：代理 mac 手写 NIO 握手 / win 用 SSH.NET 内置；桥 mac 用 NWListener / win 用 HttpListener）。
**三端一致性提醒**：mac 的 `SFTPPanel` 已让 SFTP 与 SSH **共用同一套凭据**（私钥+密码+代理）；Windows 侧 `CreateSftpClient()` 复用同一 `ConnectionInfo`，天然一致。

### 旧版文件说明
- `App.xaml(.cs)` — 启动 + **Window 级隐式样式资源字典**（统一深色皮，重写 Button/TabItem ControlTemplate）。
- `MainWindow.xaml(.cs)` — 五区布局。**关键技巧**：会话 tab 头与终端内容跨 Grid 单元拆分——把 `Sessions` TabControl 用 ControlTemplate 重写为**只渲染标签头**放顶栏，终端由 work-center 的 `ContentControl Content={Binding SelectedContent}` 承载；底部坞独立于会话。协调 SFTP/命令面板的活动会话。
- `TerminalSession.cs` — **每会话一份**的 SSH↔xterm 桥：独占 WebView2 + SSH.NET SshClient + ShellStream + 后台读线程 + 反射 PTY resize(`SendWindowChangeRequest`) + `CreateSftpClient()`（复用同凭据开 SFTP）+ `SendText()`。
- `SftpPanel.xaml(.cs)` — SFTP 双栏（本地 System.IO | 远端 SftpClient），凭据复用活动会话，目录优先。
- `CommandPanel.xaml(.cs)` — 命令板（输入+7 快捷命令 → 活动会话 ShellStream）。
- `HostEditWindow.xaml(.cs)` — 主机新建/编辑对话框。
- `Storage.cs` — `HostEntry` + `HostStore`(JSON `%APPDATA%\PixShell\hosts.json`) + `CredentialStore`(**DPAPI** 加密 `creds.dat`，CurrentUser scope，密码不落 hosts.json)。
- `web/terminal.html` + `xterm.js`/`xterm.css`/`addon-fit.js` — 本地内嵌 xterm（运行时不连外网）。C#↔JS 用 base64 JSON 消息（out/in/resize/title）。

## 5. Windows 命令通道（**先看这条，别再无脑 SSH**）

**那台机器的防火墙会在连开若干次 SSH 后开始拦截** —— 表现是随机超时、空输出、命令看似失败。
我曾一轮开几十次 `ssh pixwin` 把通道打死，然后基于空输出得出一堆错误结论（比如误判 `del` 失效），
越修越偏。用户原话："没几个命令就被防火墙拦了"、"搞得 ddos 攻击一样"。

**正确做法：走 SMB 文件通道，零 SSH。**
- C 盘已挂在 `/private/tmp/win146`（`//Administrator@192.168.1.146/C$`），读写文件**完全不经过 SSH**，
  往返 ~160ms。源码同步、脚本投递、读结果一律走它。
- 要在 Windows 上**执行命令**：常驻 agent `C:\_pixagent\watcher.ps1`（源码 `scripts/pixagent-watcher.ps1`）
  轮询 `in\*.job` → 执行 → 写 `out\<id>.out` → **最后**写 `out\<id>.done` 作为就绪信号
  （先 .out 后 .done，避免读到半截文件）。
- mac 侧：`scripts/winx.sh '<命令>'` 投递并等结果；`scripts/winx.sh --status` 看 agent 死活。
  超时用 `WINX_TIMEOUT=400` 覆盖。实测整套 `dotnet build` 可以跑通（0 错误 0 警告）。
- SSH **只在把 agent 拉起来时用一次**（`schtasks /run /tn PixAgent`），用完就停。

**上传脚本的编码规则（不遵守会莫名其妙解析失败）**：
- `.ps1` → **UTF-8 带 BOM**。Windows PowerShell 5.1 没 BOM 就按 ANSI(GBK) 解码，中文注释会让行边界错位。
- `.bat` → **GBK + CRLF**。cmd 按 ANSI 读字节，UTF-8 中文的尾字节会吞掉行尾 CR，把一行拆碎。

**已知环境故障**：`C:\Windows\System32\OpenSSH\ssh-keygen.exe` **是坏的**
（Windows 报"与你运行的 Windows 版本不兼容"，PE 头都读不出来）。可用的替代：
`C:\Program Files\OpenSSH\ssh-keygen.exe`（x64，可执行）、
`C:\Program Files\Git\usr\bin\ssh-keygen.exe`（x64，可执行）。
→ Windows 密钥管理的 `SshKeys.KeygenExe` 应优先探测这两个，别写死 System32 那个。

## 5.1 旧的 SSH 方式（保留备查，详见 `docs/WIN-CONN.md`）
- 机器 `192.168.1.146`，用户 `Administrator`。**密码认证会触发账户锁定**——已改**公钥免密**：
  - mac 密钥 `~/.ssh/pixshell_win`（ed25519）；公钥装在 `C:\ProgramData\ssh\administrators_authorized_keys`（管理员组走这个，ACL 已收紧到 Administrators+SYSTEM）。
  - `~/.ssh/config` 别名 `pixwin`（IdentityFile + ControlMaster auto + ControlPersist 300）。
  - 用法：`ssh pixwin "..."`、`ssh pixwin "cd /d C:\PixShell-win && dotnet build"`。免密、复用、无锁定（10/10 压测过）。
- 账户锁定阈值已设「从不」(`net accounts /lockoutthreshold:0`)。
- **服务端 SFTP 修复**：sshd_config 原 `Subsystem sftp sftp-server.exe`（相对路径，sshd 工作目录 System32 找不到）→ 改绝对路径 `C:/Windows/System32/OpenSSH/sftp-server.exe` 并重启 sshd。这是当初 app SFTP「远端未连接」的真根因。

## 6. 关键决策 / 坑（接手必读）
- **改 UI 前必须读老仓库源码**：`/Volumes/d/pixshell` 的 `packages/app/renderer/index.html`(结构)、`app.js`(交互 9800 行)、`代码说明概要.md`(§7 前端架构)、`docs/ui/*`。照着截图猜会把功能接错（顶栏宫格曾被错接成"快速连接"，实际是工具面板）。
- **弹窗头部拖动手势会吞按钮点击**：`NSPanGestureRecognizer` 直接加在含按钮的头部 → 「关闭」点不动（系统信息弹窗关不掉）。用 `HeaderPanGesture`（delegate 里命中 `NSControl` 就不识别）。所有弹窗另加"点遮罩关闭 + Esc 关闭"。
- **侧栏不能用 NSSplitView 折叠**：autolayout 下 `setPosition(0)` 会被侧栏内部约束顶回去（实测 `isHidden=true` 后宽度仍是 210）。改为**普通容器 + 宽度约束**(`sideWidthC`)：折叠=26pt 窄轨、`masksToBounds` 裁内容；同理坞用 `dockHeightC` 高度→0 整块消失。
- **折叠必须是"整块消失"**：只把面板体 `isHidden` 会留下空白 tab 行，看着像没收起。
- **AutoLayout 跨父视图约束会直接崩**：`v.widthAnchor.constraint(equalTo: grid.widthAnchor)` 必须在 `grid.addArrangedSubview(v)` **之后**激活，否则「没有共同祖先」抛 NSException → SIGSEGV。系统信息面板曾因此一点就崩（表现像"按钮没反应"）。**先加子视图，再加约束。**
- **回调用 `String?` 表达"成功/失败"必踩坑**：桥的 SFTP 上传/下载曾经协议写"返回路径，nil=失败"，实现却按"nil=成功、非 nil=错误信息"写，
  结果**上传其实成功了却报 `upload failed`**，反过来真出错时会报成功。已统一成 `Result<String, Error>`（成功带路径、失败带错误），语义不可能再误读。
  教训：跨模块回调别用可选值兼表两种含义。
- **面板 `open()/show()` 必须自己 `isHidden = false`**：EditorPanel 起初没有，日志显示文件已读到、回调已触发，但界面什么都没有。新增弹层时务必自查这一行。
- **排查顺序**：先看 `~/Library/Application Support/PixShell/logs/pixshell-runtime.log`，再看 `~/Library/Logs/DiagnosticReports/PixShell-*.ips`（崩溃栈能直接定位到函数）。日志比截图可靠得多。
- **GUI 测试别用 `tell application "PixShell" to activate`**：那会按名字启动 `dist/PixShell.app` 里的**旧包**，导致截到旧 UI 白忙。要用 `tell (first process whose unix id is <pid>)`，或直接按 PID 前置。
- **自绘 UI 无法用 AX 驱动**（`entire contents` 返回 0）：验证交互用 CGEvent 合成真实点击（`/tmp/click.swift`），或用 `NSBitmapImageRep.colorAt` **采样像素**确认配色（肉眼看截图会误判明暗）。
- **mac 语义高亮不套实时 PTY 流**：直接对原始字节流做正则重上色会破坏终端（编码/部分转义/TUI 光标）。`SemanticHighlight` 已移植但**未接** didReceive；didReceive 直接 `feed(byteArray:)`。后续要接需「整行完成后 / 仅非 alt-screen」。
- **mac SFTP 用自研 v3 而非 Citadel**：避免引入会改 swift-nio-ssh 版本 pin 的依赖、不破坏现有 SSH。
- **密码不落明文**：mac=Keychain，win=DPAPI。hosts.json 只存元数据。
- **openSession 里 `sess.password` 必须在 `selectSession` 之前赋值**（selectSession 会触发 SFTP 面板用它连接；曾因顺序错导致 SFTP nil 密码认证失败）。
- **mac stdout 缓冲**：`setbuf(stdout, nil)`（否则 print 被缓冲，kill 时丢失）。
- **mac 截图**：GUI 窗口常被 Chrome 抢焦点；用 CGWindowID 精确截图（`/tmp/winid.swift` 找 owner=PixShell 的窗口，`screencapture -o -l<id>`）。
- **Windows GUI 无法在此截图**（SSH 会话隔离）；Windows 侧以「编译绿 + SSH.NET 真机往返自测」为验收。
- **grok-lan → codex 通道坏**（超时不落地）；本地 grok CLI 要 TTY、opencode 本地后端偶发错。**可靠通道 = Agent 子智能体 + SSH(pixwin)**。

## 7. 已验证（真机）
### 本轮真机验证补充（2026-07-27）
- **代理 SOCKS5 真链路**：用 `ssh -D 127.0.0.1:11080` 起真实 SOCKS5，主机设 `proxyId` 后连接 → 日志「代理握手成功 → 隧道已建立」→ SSH 握手成功 → 经代理执行远端命令返回 `ubuntu24`。（代理握手此前只有 loopback 假服务器测过，现已真链路验证）
- **私钥认证真链路**：生成 ed25519 装到服务器、**清空钥匙串密码**后连接 → 日志「已加载 ed25519 私钥 → 尝试私钥认证 → shell 已打开」。
- **本地桥全链路**：401/403/404 鉴权与 CSRF 防护、`/v1/app/exec` 真跑远端命令、SFTP **上传→读回→下载→本地字节比对一致**、错误路径返回真实原因。
- **编辑器**：打开远端 `/etc/ssh/sshd_config`、含中文文件读写正常。
- 以上测试产物均已清理（服务器 authorized_keys 测试公钥、临时文件、临时代理配置），钥匙串密码已还原。

- mac：原生窗口 + SwiftTerm 彩色渲染；连真实 SSH（Windows box）开 PTY shell；连接管理器（JSON+Keychain）；多会话双 tab；**SFTP 上传+下载往返自测 PASS**；深色五区布局。全部 GUI 截图证。
- Windows：`dotnet build` 绿（ssh pixwin 复核）；连接管理器（JSON+DPAPI，存储往返测过）；多会话（TabControl+每 tab 独立 TerminalSession）；**SFTP 真机往返 PASS**（list62/mkdir/upload/download 字节一致含中文/清理）；深色主题。
  **顶栏 GUI 已真机核实**（2026-07，schtasks 交互会话 + RenderTargetBitmap）：无边框自绘顶栏，窗口按钮在右侧 `— □ ✕`（Windows 顺序），主题/工具/菜单三键在其左；最大化经 `WM_GETMINMAXINFO` 钳制到工作区、顶栏不再被裁到屏幕外。注意 RDP 会话 DPI=200%，用屏幕截图验证前必须用逻辑像素而非物理像素移动窗口，否则右侧按钮会被推出画面（曾误判为"没渲染"）。

## 7.9 本轮改动（2026-07-27，UI 修复 + SSH 兼容性 + 密钥管理 + 水墨主题）

**已修的真 bug**
1. **手动断开会清掉已保存密码**（两端观感：存了密码，断开再连还要重输）。根因：`menuDisconnect` 先把
   `connected=false`，随后才收到关闭回调，于是被误判成"认证被拒"→ 删钥匙串 + 弹密码框。
   修法：`TermSession.shellOpened`（**粘性**，shell 开过就为真）替代 `connected` 做判断，外加 `closeHandled` 去重。
2. **断开重连会多开一个标签页**。旧实现 `closeSession + beginSession` = 关掉旧标签再 append 新的。
   改为 `reconnectCurrent()` / `ReconnectInPlaceAsync()`：**原地复用同一标签和同一终端视图**。
3. **整个 App 鼠标都是 I 型光标**。SwiftTerm 的 `cursorUpdate` 无条件 `NSCursor.iBeam.set()`，
   而我们没有任何视图声明过光标矩形，AppKit 无从还原。修法：窗口根视图铺一块 `.arrow` 光标矩形兜底
   （`UI/CursorFix.swift`）。SwiftTerm 那两个方法是 `public` 非 `open`，模块外**不能** override。
4. **延迟(网关 ping)整块是死的** —— 监控命令根本没采集 ping，`PushPing` 也从没人调。
   两端监控命令补 `pinghost/pingms`，标题显示 `网关 <ip> · <ms> ms`。
5. **弹窗图标是系统蓝色文件夹**（非 .app bundle 没有图标资源）。新增程序化绘制的 `UI/AppIcon.swift`
   （扁平圆角 squircle，沿用老仓库 app-icon 的终端+像素意象），并加 `NSAlert.pix()` 统一入口 ——
   只设 `NSApp.applicationIconImage` **不够**，NSAlert 必须显式赋 `icon`。
6. 连接管理器/系统信息/代理面板里的**假红绿灯圆点**（点了没反应）已删除，右侧本来就有真的「关闭」。
7. **连接管理器弹窗鼠标光标变成 I 型** 且 **不能随意拖拽**（只能在主界面内）：原实现为覆盖在根视图的 NSView 遮罩，导致鼠标被终端的 cursorUpdate 劫持；现已将 `ConnManager` 重构为 `NSWindowController` 管理的独立弹出窗口（`isMovableByWindowBackground = true` + 透明无标题栏，且隐藏了系统默认的红绿灯按钮），彻底解决光标问题且支持自由拖拽到屏幕任意位置。
8. **连接管理器搜索框常驻蓝色高亮**：禁用了 `search` 输入框的 `focusRingType = .none`，使其在激活状态下不再出现突兀的系统蓝色边框。

- 工具面板从 620×520 遮罩弹窗改回**小浮窗**（老仓库 `.flyout-panel`：360 宽 / 顶栏下方靠右 /
  点图标呼出再点收起 / 无黑幕），主体是 **120pt 下载任务框**（新增 `DownloadTasks` 存储，SFTP 下载会上报）。
  工具输出移到独立的 `ToolResultWindow`（老仓库那几个动作本来就是开成中央标签页的）。
- 快速连接页面**美化与真实系统图标**：优化了落地页头部标题字号和卡片间距尺寸，并为卡片增加了层次感阴影（Shadow）。同时通过脚本自动拉取了 Ubuntu/Debian/CentOS/Windows/macOS/OpenWRT 等十多种常见系统的 SVG 图标集成到了 `Assets.xcassets`，取代了粗糙的系统自带 SF Symbols（如六边形或圆圈），现在初次连接成功识别出系统后，卡片将展示原汁原味的官方系统 Logo。
- 连接过程不再往终端 feed「连接中…」（终端只留远端真实输出），改为 Termius 式**连接动画**覆盖层
  （脉冲圆点 + 主机名 + 分步文案 + 不确定进度条）。
- 侧栏「已连接/已断开」变成**可操作按钮**（红绿灯 + 断开/连接）。
- 认证失败**当场重弹密码框**（带记住密码），不再只丢一行报错。
- 首次连上自动识别远端发行版写回 `osId`，主机卡片图标/配色随之变化。

**SSH 兼容性（重要，接手必读）**
`swift-nio-ssh` 支持的算法非常窄：主机密钥只有 ed25519/ecdsa（**完全没有 RSA**）、加密只有 aes-gcm、
KEX 只有 curve25519/ecdh、认证只有 password/publickey。只提供 RSA 主机密钥 + aes-ctr + dh-group14 的
老机器（CentOS 7 / 老 OpenSSH / 交换机路由器）**根本连不上**。
`NIOSSHPublicKey.BackingKey` 是 `internal enum`，模块外无法扩展；而且只补 RSA 没用 ——
那批机器通常 RSA + ctr + dh-group 三样都要，等于把 OpenSSH 重写一遍。
**采用混合方案**：默认仍走 nio-ssh（今天能连的零回归）；只有当会话在 shell 打开**之前**因**算法协商**
失败关闭时（`looksLikeAlgorithmMismatch`），自动改用系统 `/usr/bin/ssh` 重连一次（`SSH/OpenSSHSession.swift`，
每会话最多回落一次）。回落路径白拿 OpenSSH 全部能力：全算法、keyboard-interactive、2FA、证书、
`~/.ssh/config`、ProxyJump、ssh-agent。
> **坑（实测踩过）**：必须用 `forkpty`，不能用 `posix_spawn + POSIX_SPAWN_SETSID + dup2(slave)` ——
> 后者只把从端接到 0/1/2，**没有设成控制终端**（缺 TIOCSCTTY），ssh 无法交互提问，会直接
> 打印 `Permission denied` 退出。`forkpty` 内部做了 setsid + TIOCSCTTY。
> 回落路径**不自动填密码**（不用 SSH_ASKPASS/sshpass）：提示原样出现在终端里由用户回答，和平时用 ssh 一致。
Windows 侧 SSH.NET 2024.2 算法本来就宽，只补了 **keyboard-interactive**（很多服务器关了密码认证只留它）。

**新功能**
- **密钥管理**（`Store/SSHKeys.swift` + `UI/KeyManager.swift`，菜单 文件 → 密钥管理…）：
  列出 ~/.ssh 私钥（类型/位数/指纹/注释/是否带口令），生成（Ed25519 / RSA4096 / ECDSA）、复制公钥、
  用于此主机、在访达中显示、删除。生成一律调系统 `ssh-keygen`，不自己实现密钥生成。
- **水墨 / 复古 两套中国风浅色主题**：`Theme` 从 `dark: Bool` 升级为四态
  `Theme.Kind{dark,light,ink,retro}`（`Theme.dark` 保留为计算属性，两者都算浅底，所有老调用点不变）。
  顶栏主题按钮 深色→浅色→水墨→复古 循环。
  > **两者别搞混（第一版做错过）**：
  > - **水墨** = 真·水墨画：**冷调近乎单色的灰白**（远山薄雾），bg `#eef1f2` / text `#1f2528` /
  >   accent 焦墨青 `#4a5a61`，整幅**只有印章朱砂 `#9c3d35`** 一点红（同时兼作 err，保证出错仍醒目）。
  >   终端方案 `ink_wash`「水墨」的 ANSI 基本是一整套**墨阶**，只有 red 位留朱砂。
  > - **复古** = 暖米色宣纸 `#f5f2ea` + 靛青/赭黄/青绿等矿物颜料，终端方案 `retro_paper`「复古」。
  >   这是做旧调，**不是水墨** —— 第一版把它当水墨交付了，用户一眼看出不对。

  **主题切换语义（别改回轮播）**：设置里的四选一是"选定当前主题"；选中浅色系的任意一套
  （浅色/水墨/复古）会同时把它记为 `Theme.lightKind`（持久化 `pixshell.lightKind`）=
  **这台机器的"浅色"是哪一套**。顶栏主题按钮此后只在 **深色 ⇄ lightKind** 两态之间切，
  不再挨个轮一遍（用户明确要求：「不要每个都要切换一遍」）。`Theme.dark = false` 也会切到 lightKind。

- **左栏「文件 / 对话」模式切换**（`UI/AgentChatView.swift`）：SFTP 面板左栏（本地侧）加了一个
  分段控件，可在「本地文件浏览」和「与本机 CLI agent 对话」之间切。左栏**默认就是隐藏的**
  （`localHidden = true`，只显示远端），用坞里的按钮呼出。
  对话走**非交互一次性调用**（`claude -p <prompt>` / `codex exec <prompt>`），工作目录 = 左栏当前本地路径，
  所以 agent 能看到你正在浏览的那批文件；最近 6 轮拼进 prompt 当简易上下文（一次性调用没有服务端会话）。
  正在跑时「发送」变「停止」。
  > **坑**：GUI 进程的 PATH 比登录 shell 窄，`claude` 常装在 `~/.local/bin` 里 —— `which()` 必须
  > 额外补 `~/.local/bin`、`~/bin`、`/opt/homebrew/bin`、`/usr/local/bin`，否则界面上会显示"未安装"。
  > 已用与之完全相同的调用形态实测通过（`claude -p` 返回正常）。
- **自定义高亮/普通文字颜色**：设置里两个取色器，持久化 `pixshell.hl.color` / `pixshell.hl.plainColor`，
  留空=跟随主题（默认，行为不变）。见 `Store/HighlightColors.swift`。

**AI 直接操作本软件 / CLI 无头复用会话**（`Bridge/AgentCLI.swift`）：
桥（127.0.0.1:8766）本来就是**在已连接的那条 SSH 会话上**干活 —— 也就是说 agent 每发一条指令
**不会**新建 SSH 连接，这正是"无头调用、不用每条都重连"要的效果。本轮把它包成了命令行：
App 启动时把 `pixshell` 脚本写到 `~/Library/Application Support/PixShell/bin/pixshell`，子命令：
`sessions / screen [行数] / exec <命令> / type <文本> / hosts / sftp-ls [路径]`
（`PIXSHELL_SESSION` 指定会话序号）。左栏对话面板会把用法拼进 prompt，所以 agent 能自己
**读终端画面 + 像人一样往终端敲字 + 跑命令**。
> **token 不进 prompt**：脚本自己去读 `agent_token` 文件。写进提示词的话它会被一并发到模型服务商，没必要。
> 实测通过：`exec "hostname; uptime"` 拿到真实输出；`type "echo AI-DROVE-THIS"` 在终端里真的敲进去并执行了。
> **坑**：`python3 -c` 的单行程序里**不能**指望 `\n` —— 经 Swift→shell 两层转义后会变成字面量反斜杠 n，
> python 直接语法错。所有内嵌 python 一律写成**纯分号单行表达式**。

**修：汉堡菜单「密钥管理器」点开的是连接管理器**。`menuKeyMgr()` 是早期没有独立密钥界面时
写的兜底（开连接管理器/主机编辑）。KeyManager 做出来之后忘了把它接过去，导致名实不符。
已改为 `menuKeyMgr() { openKeyManager() }`。
> 教训：新做了独立界面，要回头把**所有**指向旧兜底实现的入口一并改掉，别只接新加的那个菜单项。

**Windows 补齐语义高亮**（`Highlight/SemanticHighlight.cs`，从 mac 版 1:1 移植 ~400 行）。
此前 Windows 端**完全没有**这个能力，是两端一处真实功能差；补上后「自定义高亮/普通文字颜色」
（`Store/HighlightColors.cs` + 设置里两个色值输入 + 语义高亮开关）和「cargo 输出着色」才有宿主可挂。
接入点：`TerminalSession` 读到字节后先 `Decorate` 再送 xterm，关掉开关就走原始字节。

**借鉴 tailspin 补了 5 类规则**（两端同步）：UUID（必须排在 hash 规则前，否则被 8-40 位 hex 吃掉半截）、
HTTP 方法、进程名`[pid]`、时长（`12ms`/`1.5s`，数字与单位间不留空格以免误判 "5 m"）、
引号字符串（放最后，内部已着色的片段不会被整段吞掉）。新增调色板项 `str`。
> **为什么不直接用 tailspin**：它是 Rust crate/CLI 且**行式处理**；我们是活的 PTY 流
> （半行、光标控制、top/vim 全屏重绘），行式高亮会把交互式程序画面搞坏，还会把刚搁置的 Rust 依赖引回来。
> 所以只借它的类别清单，架构保持"进程内 + 保留已有 ANSI 转义 + 只给纯文本段上色"。

**验证**（隔离测试工程，非只看编译）：10 条纯文本样本全部着色，且**脱掉 SGR 后与原文逐字相同**
（高亮器绝不能改动文本本身，这条比"有没有颜色"更重要）；含 ANSI 的输入原有 `31m`/`0m` 原样保留、
新颜色只加在纯文本段。
> 第一版测试把两类样本混在一起算，把"含 ANSI 的那条"误判成 MISMATCH —— 是**测试写错**不是代码错，
> 收紧成两类分别断言后全部通过。

**修：Windows `启动 PixShell.bat` 跑不起来**（报 `'p' 不是内部或外部命令` + 中文乱码）。
根因**不是**行尾，是**编码**：cmd.exe 按当前 ANSI 代码页（中文 Windows = GBK/936）读 .bat 的字节。
文件存成 UTF-8 时，中文字符的尾字节常常正好是 GBK 的**前导字节**，会把紧随其后的 `\r` 吞掉 ——
行边界错位，`REM` 失效、半行被当成命令执行。
例：`。` = UTF-8 `E3 80 82`，GBK 读到 `82` 当前导字节，吃掉下一个字节（正是 CR）。
**结论：.bat 必须存成 GBK + CRLF，且不要加 `chcp 65001`**（那会让 GBK 文本反过来显示成乱码）。
> 排查手法值得记：把 `@echo off` 改成 `@echo on` 跑一遍，逐行回显立刻能看出是哪一行被吞了 ——
> 比盯着文件猜快得多。第一版我以为是裸 LF（确实也有，10 个），改完 CRLF 仍报错，才发现真凶是编码。

**和现有 AI 工具无缝联动**（用户要求：能和 CLI 工具 / 桌面 AI 应用直接对接）：
- **MCP server**（`Bridge/AgentMCP.swift`）：把 PixShell 暴露成 MCP 服务器，Claude Desktop /
  Claude Code CLI / Cursor 等支持 MCP 的客户端一次注册就能把它当工具用。
  工具：`list_sessions / read_screen / exec_command / type_text / list_hosts / sftp_list`。
  实现取巧：MCP over stdio 就是行分隔 JSON-RPC，不需要 SDK；python 壳子只管协议，
  真正干活转手给 `pixshell` 命令 —— 两条路径共用同一套桥调用，行为不会漂。
- **PATH 软链**：`pixshell` / `pixshell-mcp` 软链到 `~/.local/bin`（用户自己的 bin，不用 sudo）。
  只在目标不存在、或已是指向我们的旧软链时才写，**绝不覆盖用户同名文件**。
- 菜单 帮助 → **接入 AI 工具…**：给出 `claude mcp add` 一行命令 + Desktop 的 JSON 片段，一键复制。
  故意**不**替用户改他的 Claude Desktop 配置文件。

> **大负载必须截断（用户明确提过 MCP 大数据传输老报错）**：MCP 客户端对单条工具结果大小很敏感，
> 一次回几 MB 基本必炸。所以在**协议层统一截断**：默认 60KB / 上限 200KB，**头 60% + 尾 35% 都留**
> （报错信息常在末尾，只留头会把关键行切没），并明确写出"略去多少字节、共多少字节、怎么拿更多"。
> `read_screen` 的 lines 也钳到 ≤2000。实测：1.35MB 输出 → 57KB，**调用成功**而非失败。
> **坑**：给 python 嵌路径**不能用 JSONSerialization** —— 它把 `/` 转义成 `\/`，python 不认这个转义，
> 路径会真的多出反斜杠而失效（实测 `No such file or directory '\/Users\/…'`）。只需转义反斜杠和引号。

**左栏机器人按钮**（`UI/RobotIcon.swift`）：坞图标行、刷新左边。SF Symbols 没有机器人符号、
emoji 🤖 是彩色跟不了主题，所以自绘成 **template image**（只有形状+alpha），由 `contentTintColor`
着色 —— 四套主题下自动搭配。交互：点一下进对话，**再点一下退出并还原进来之前的样子**
（原来只有远端就回只有远端，原来远端+本地就回远端+本地，见 `localHiddenBeforeChat`）。

**Rust 支持**（用户要求"原生支持"）：
- 编辑器（`UI/EditorSyntax.swift`）：`.rs` 映射和关键字**本来就有**；本轮补齐了漏掉的关键字
  （in/as/break/continue/type/extern/常用基础类型/Ok/Err/Some/None…）并加了 Rust 专属结构着色：
  属性 `#[derive(...)]`/`#![no_std]`、生命周期 `'a`/`'static`、宏调用 `println!`、
  原始字符串 `r"…"`/`r#"…"#`、大驼峰类型名。
  > **坑**：原始字符串那条正则里含 `"#`，Swift 里必须用 `##"…"##` 定界，否则 unterminated string literal。
- 终端输出（`Highlight/SemanticHighlight.swift`）：认 cargo/rustc 的特有形状 ——
  `error[E0382]` 整体标红（不是只中"error"）、`--> src/main.rs:12:9` 按路径色、`thread 'main' panicked at`、
  `... ok` / `... FAILED` / `test result:`、以及 `Compiling|Finished|Running|…` 行首动词。
  这些**排在通用 error/warning 词表之前**，避免被粗粒度规则先吃掉。已用 8 条真实 cargo 输出样本逐行验证着色。
- 快捷命令新增「Rust」分组：cargo build/test/clippy/fmt --check/run + 工具链版本（`rustc -Vv; cargo -V; rustup show`）。
- **未做**：把核心模块改写成 Rust（共享原生库）。那是架构级改动，需要单独规划，见 §8 TODO。

**Windows 同步进度（本轮）**：已同步 —— 原地重连、认证失败重弹密码框、侧栏连接状态按钮、
延迟(网关 ping)、工具浮窗 360 小窗 + 下载任务列表 + 独立结果窗、连接动画、远端系统识别、
keyboard-interactive、**密钥管理**（`Store/SshKeys.cs` + `UI/KeyManagerWindow.cs` + 文件菜单）。
**主题也已同步**：`Theme/ThemeInk.xaml` + `Theme/ThemeRetro.xaml` 两份令牌字典、
`ThemeManager` 升级为四态 `Kind{Dark,Light,Ink,Retro}` + `LightKind` 语义 + `theme.json` 持久化
（此前 Windows 端主题**完全没持久化**，每次启动都回深色，顺带修了）、设置四选一、
终端配色加 `ink_wash`/`retro_paper`。
**本轮又补同步**：Rust 编辑器增强（属性/生命周期/宏/原始字符串/关键字补齐）、cargo 快捷命令、
**MCP server + CLI**（`Bridge/AgentCLI.cs` 生成 `pixshell.cmd` + `pixshell.py`；同一个 py 两用 ——
带 `--mcp` 是 MCP server，否则是命令行）、帮助菜单「接入 AI 工具…」。
MCP 已在 Windows 真机验证：`initialize` + `tools/list` 六个工具全部正常返回。
`pixshell.cmd` 里加了 `chcp 65001` + `PYTHONIOENCODING=utf-8`，否则中文输出在控制台是乱码
（注意：这是 **.cmd 包装器**的做法，和上面「.bat 必须存 GBK」不冲突 —— 包装器本身是纯 ASCII）。
**Windows 已全部补齐**：左栏对话面板（`UI/AgentChatView.cs`，纯代码构建）+ 机器人按钮
（`UI/RobotIcon.cs`，几何图形 + `BrushMuted` 填充，四套主题自动搭配；Segoe MDL2 没有机器人字形、
emoji 🤖 是彩色跟不了主题）。交互与 mac 一致：点一下进对话，再点一下退出并**还原进来之前的样子**
（`_localHiddenBeforeChat`）。`AgentCLI.PromptPreamble()` 同样把「你可以操作本软件」的用法拼进 prompt。
Windows 的 `Which()` 除 PATH 外还补查 `~/.local/bin`、`~/bin`、`%APPDATA%\npm`，
并按 `.cmd/.exe/.bat` 依次探测（GUI 进程 PATH 比登录 shell 窄，且 npm 装的 CLI 是 .cmd）。

> **Windows 端没有语义高亮**：没有 `Highlight/` 目录，`terminal.html` 与 C# 侧都没有着色逻辑。
> 所以"自定义高亮/普通文字颜色"在 Windows 上**没有宿主可挂** —— 要先把 mac 的
> `SemanticHighlight.swift`（~330 行正则着色器）整体移植过去才谈得上。这是一处真实的两端能力差。

> **密钥管理（Windows）只做到编译通过，未做运行时验证**：远端 SSH 会话里
> `C:\Windows\System32\OpenSSH\ssh-keygen.exe` 执行报"不是有效的应用程序"（文件存在、203KB、
> PowerShell 是 64 位），原因未查清；也可能只是无桌面会话的执行上下文问题。
> 代码里已按 PATH → System32\OpenSSH 顺序解析，**下次接手请在真机 GUI 里点一次「生成密钥」确认**。

**注意**：本轮子代理全部被 API 安全分类器拦下（这个仓库上下文会触发，连纯 UI 的主题任务也拦），
所有改动均由主会话手写完成 —— 别再指望用子代理并行加速这个仓库。

## 7.95 日志覆盖（被用户批评后补齐，2026-07-27）

**原状**：App 内的日志一直有、也一直在用（本轮"断开重连要重输密码"就是靠
`[WARN] 认证失败/无法连接…已清除保存的密码` 两行定位的）。**但本轮新写的那批代码基本没埋点**：
`AgentChatView` 0 处、`KeyManager` 0 处、`OpenSSHSession` 仅 1 处，
而 `pixshell` CLI / MCP server 是**进程外**、一行都不写 —— 所以 Windows 上
`ssh-keygen` 跑不起来、`.bat` 报错时只能靠 `@echo on` 逐行回显去猜。这是真实缺陷。

**已补（两端）**：
- `OpenSSHSession`：启动 pid/fd/目标、读取 errno、**退出码**（255=ssh 自身失败，1=远端命令失败）、exec 启动失败。
- `AgentChatView`：提问（agent/cwd/prompt 长度）、进程 pid、**非 0 退出码**、返回字数
  （空输出额外提示"检查 agent 是否需要登录/额度"）、用户取消。
- `KeyManager`：生成请求/失败、复制公钥、读不到公钥、选用密钥。
- **进程外脚本自己写文件日志**：`logs/agent-cli.log`、`logs/agent-mcp.log`，
  每次调用一行，HTTP 非 200 连响应体一起记；超 2MB 自动砍前一半。
  > MCP server 由客户端拉起，stderr 通常被吞掉 —— 不落文件就等于没法查。

**顺带查出一个真 bug（正是加日志才暴露的）**：桥的 `exec`/`screen` 对**不存在的会话号**
返回 `HTTP 200 + ok:true + 空输出`，调用方根本分不清"命令没输出"和"会话号写错了"。
已改为 404 + 明确文案（`会话 99 不存在（当前共 1 个，用 /v1/app/sessions 查）`），两端同步。
实测三层都留痕：桥 `[WARN] exec 指定的会话不存在 session=99（共 1 个）`、
CLI `POST exec HTTP=404 body={...}`。

**教训**：写完功能顺手在"会失败的地方"埋点，别等出事再补 —— 尤其是**进程外**的东西。

## 7.96 本轮改动（2026-07-27 后半段：命令面板重做 + 文本编辑器独立窗口）

### 命令面板 `UI/CommandPanel.swift`（照老仓库重做为左右分栏）

之前是「分类 pill 一行 + 命令 chips 横向滚动一行 + 编辑器压在下面」，命令被裁掉半截，
用户评价"完全不可用"。现在的结构：

```
[📁全部 📁系统 📁服务 …排满自动换行]                        [＋新建]
┌────────────────────────────┬─┬──────────────────────┐
│ 磁盘⚙  内存⚙  进程TOP⚙ …    │ │ 命令编辑器 选项 ⟩收起 │
│ Nginx重载⚙  看日志⚙ …       │ │ ┌──────────────────┐ │
├────────────────────────────┤ │ └──────────────────┘ │
│      发送到[当前会话] [发送] │ │ [下拉] [发送]        │
└────────────────────────────┘ └──────────────────────┘
```

- **`UI/FlowView.swift`（新）**：自动换行的流式容器。AppKit 没有现成的，NSStackView 只会挤成一行。
  高度取决于**宽度**，而 `intrinsicContentSize` 拿不到最终宽度 → 只能在 `layout()` 里算完写回自管约束。
- 分类 chip 图标必须用 **SF Symbol 图片**，不要用 `🗀` 这类字形 —— 系统字体没那些码位，渲染成 `?` 豆腐块。
- 右栏宽 **213pt**（原 320，用户要求收掉三分之一）。收窄后底部塞不下 `[选项]+发送到+下拉+[发送]`，
  所以「选项」挪到标题行、底部只留 `[下拉][发送]`。
- 交互：**单击** = 选中并载入编辑器；**双击** = 直接发送（`NSApp.currentEvent.clickCount` 判断）。
  分两级是避免误触就把命令打到服务器上。
- **「收起」必须留可点的东西**：第一版收起后右栏只剩 30pt，收起按钮连同标题一起被挤出可见区，
  再也打不开。现在收起态显示一个**占满窄栏的展开按钮**。
- 右键菜单（老仓库叫"文件夹"，这里统一叫**分组** —— 同一个东西，别照抄叫法）：
  命令上 `添加命令… / 复制命令 / 编辑 / 删除 / 新建分组… / 移动到▸`；
  空白处 `新建分组… / 添加命令…`；分组 chip 上多 `重命名分组… / 删除分组`。
  **「复制命令」= 复制命令文本到剪贴板**，不是复制出一条副本（第一版做错过）。

### `Store/QuickCommands.swift`：分组要单独持久化

`groups()` 原本纯从命令的 `group` 字段**派生** → 新建一个还没放命令的空分组会立刻消失。
现在额外存一份 `quick-command-groups.json`（`emptyGroups`），`groups()` 取 显式 ∪ 派生。
新增 `addGroup / removeGroup / renameGroup / move`。删分组时里面的命令移回「默认」，**不连带删命令**。

### `NSTextView` 当 `NSScrollView.documentView`：踩了两次的坑

**症状分三级，很容易误判为"设值没生效"**：
1. 什么都不配 → textView frame 宽度 **0**、textContainer 宽度**负数**。
   `string` 写得进、读回来长度也对，但**一个字都不显示**（实测 `frame=0x89 container=-12`）。
2. 改用 Auto Layout 把 documentView 钉在 clipView 上（top/width、故意不钉 bottom）
   → 第一行显示了，**但高度不跟内容长，第二行被裁**（实测 `frame=198x27`，2 行内容只显示 1 行）。
3. 正解：用 AppKit 自己的工厂 `NSTextView.scrollableTextView()`，封成
   **`ScrollableText.make(font:editable:bg:border:)`**（`UI/Components.swift`）。
   实测 62 字符 4 个换行片段 → `frame=198x72`，全显示。

> 根子上：documentView 的尺寸该由 NSTextView 的 `isVerticallyResizable` 机制管，
> 拿约束去按它必然打架。**别再手搓。**

受影响并已统一改用工厂的三处：命令编辑器、`AgentChatView` 对话记录、`ToolResultWindow` 输出区
（后两处当时同样只能显示第一行）。`EditorPanel` 是唯一一开始就配对的，所以它不用改。

### 文本编辑器 `UI/EditorPanel.swift`

- **改成独立 `NSWindow`**（`AppDelegate.editorWindow` + `showEditorWindow(title:)`）。
  原来是四边钉在 root 上的内嵌浮层 —— 既拖不动也不可能拖出主窗，而改远端文件时经常要挪开对照终端。
  关掉只 `orderOut`，复用同一窗口（保留尺寸/位置）；`isReleasedWhenClosed = false`，否则下次开是野指针。
- **卡片必须填满面板**：它原本是固定 `860×620` + 居中的弹层卡片，搬进窗口后窗口一缩小内容就被裁
  —— 这就是"缩放后字都看不见"。已改为四边钉住。
- **红绿灯隐藏**（头部已有自己的「关闭」，两套入口冗余）：`standardWindowButton(...)?.isHidden = true`。
- **系统标题栏也去掉**（面板头部已有文件名+语言徽章+保存/关闭，再顶一条纯属浪费高度）：
  `styleMask.insert(.fullSizeContentView)` + `titlebarAppearsTransparent = true`
  + `titleVisibility = .hidden`，再加 `isMovableByWindowBackground = true`。
  视觉上没有那条框，但标题栏**拖拽区仍在**，窗口照样能拖能缩放；
  点在按钮/文本视图上不会误触发拖动（AppKit 让控件优先）。
- **保存必须回报结果**。`onSave` 从 `((String) -> Void)` 改成
  `((String, @escaping (String?) -> Void) -> Void)`（nil = 成功）。
  修掉一个**会丢数据的 bug**：原来 `saveAction()` 先 `isDirty = false` 再发起保存，
  关闭确认里点"保存"还会立刻 `onClose()` —— 远端写失败时改动静默丢失，失败弹窗还在窗口关掉之后才出现。
  现在**只有成功才清脏标记 / 才关闭**，失败就留在编辑器里显示原因。
- 保存提示写在**编辑器头部**（绿 `已保存 HH:mm:ss` / 红 `保存失败：…`）。
  原来写到主窗底部状态栏，而编辑器把整窗盖住，用户永远看不见。

### 光标 `UI/CursorFix.swift`：cursor rect → cursorUpdate

SwiftTerm 无条件 `NSCursor.iBeam.set()`，是命令式调用、不参与还原，所以 I 型会粘着走。
三版演进（**前两版都错，别退回去**）：

| 版本 | 做法 | 结果 |
|---|---|---|
| v1 | 根视图铺一块 `.arrow` cursor **rect** 兜底 | 文本输入区也被兜成箭头，编辑器里不显示 I 型 |
| v2 | 再给文本视图补一块 `.iBeam` **rect** | I 型**离开文本区照样粘着**，等于把老 bug 带回来 |
| v3 | `cursorUpdate` + 覆盖全窗的 tracking area | ✅ 每次移动重新分发，不粘 |

`cursorUpdate` 由 AppKit 按**指针下最深的那个装了 cursorUpdate 追踪区的视图**分发：
终端/文本视图自己的赢（I 型），其它 chrome 落到 `ArrowRootView` 兜底（箭头）。
实测 `iBeam`（指针在编辑器文本区）→ `arrow`（移到主窗侧栏）。

### 本轮的操作事故（都是我造成的，记下来别重犯）

1. **拿用户真机配置当测试夹具**：为测主题切换手动 `defaults write PixShell pixshell.lightKind ink`，
   导致用户点主题按钮出来的是水墨而不是浅色，"整个 UI 都变了"。
   → 测持久化偏好要用独立域或临时 HOME。
2. **盲点坐标做合成点击**：`osascript click at {x,y}` 有一次点中顶栏主题按钮，
   把 `pixshell.theme` 写成 `light`。→ 不要盲点；要点就先确认目标控件位置。
3. **用下标做文本切除，删掉了真实代码**：`j = s.index('root.addSubview', i)` 定位到了
   `root.addSubview(proxyPanel)`，把中间 `proxyPanel = ProxyPanel(...)` 的创建一起切掉 → 启动崩
   `Fatal error: Unexpectedly found nil`（`AppDelegate+Layout.swift:266`）。
   第二次清理又切掉了 `performSave/setSaveStatus`。
   → **只用锚点替换，不用下标切除**；改完跑一次"引用了但从未创建的字段"自查。

### 7.97 深度 UI 打磨与视觉交互升级 (2026-07-27)

1. **终端高亮色彩优化**：修复了在深色模式下，原本由语义高亮引擎生成的青色文本（如 IP、时间戳）对比度过低、看不清的问题，统一修改为醒目的红色，大幅提升了终端在暗色系下的可读性。
2. **水墨主题与玻璃拟物化质感（Glassmorphism）**：
   - 彻底优化了「快速连接」落地页。将面板背景彻底透明化，以透出底层隐约映射桌面壁纸颜色的 `Ambient Blur` 毛玻璃环境光（NSVisualEffectView），实现真正的“艺术画布”质感。
   - 为主机卡片加入基于 `CASpringAnimation` 的 3D 物理悬浮微动效（Hover Physics）。鼠标悬停时卡片会伴随阻尼动画微微放大（1.02倍），并浮现纵深投影，彻底摆脱死板的颜色变化。
3. **全局水波涟漪过渡动效（Ripple Effect）**：
   - 弃用了原生 AppKit 生硬的交叉溶解（Fade）。在所有主题下，新建或切换主机连接时，均会触发底层 CoreAnimation 的 `rippleEffect` 水波涟漪滤镜。
   - **水墨主题专属节奏**：涟漪动画长达 0.85 秒，配合缓入缓出时间曲线，模拟毛笔拂过水面、留下短暂涟漪并逐渐消散的东方美学。
   - **其他主题**：涟漪时间设为 0.6 秒，兼顾视觉体验与生产力工具所需的干脆利落。
4. **修复 SFTP 幽灵残留文件树**：
   - 修复了关闭所有会话退回到“快速连接”落地页后，底栏文件树仍显示上一次连接的远端文件系统的 bug。
   - 在 `AppDelegate+Sessions.swift` 的 `closeSession` 中补充了 `sftpPanel?.disconnect()` 的清理逻辑，保证零会话时文件系统状态同步清空。

### 尚未验证 / 未同步

- **语法高亮已验证**（2026-07-27）：开 `/tmp/pix_demo.sh` 实测
  `语言=shell 字数=134 前景色种数=5`，截图确认注释/关键字/字符串/数字分别着色。
  注意 `.txt` **本来就没有语法可高亮** —— 之前误判"高亮没做"就是因为拿 `.txt` 试的。
- **Windows 端本轮全部未同步**：命令面板重做、编辑器独立窗口/保存回报/光标、ScrollableText。
- 参考项目 [notepad--](https://github.com/cxasm/notepad--) 是 **C++/Qt + Scintilla**，
  代码无法直接移植（Scintilla 是整套自绘编辑控件框架），且 GPL 会污染许可。
  **可借鉴的是功能清单**：编码自动识别（GB18030/UTF-8 BOM）、大文件分块加载、
  括号匹配、语法折叠、列编辑、书签。我们目前只有正则着色 + 行号 + 查找替换。

## 8. TODO（真实剩余缺口 · 已对照老仓库与两端实现核实，2026-07 更新）
> 3.5 功能矩阵是权威完成度表；此处只列**仍未做**的项。私钥认证 / 代理隧道 / 监控面板 /
> SFTP 智能打包 / agent-bridge CLI / RDP 均已完成（勿再列为待办）。

1. **Cloud OAuth 备份**（两端）：GitHub Device Flow + 四家云盘。阻塞——老仓库 cloud-oauth.js 的
   GitHub `client_id` 是占位符，无法真连。**本地包 + WebDAV 备份已可用**，此项为老仓库自身也未真正打通的能力。
2. **Windows 软件更新检查未接**（mac `AppUpdate.swift` 已有检查逻辑）。
3. **分发打包**：mac 正式签名/公证；Windows 自包含 exe / MSIX。
4. #18 多地点 ping —— 老仓库未实现，暂不做（非迁移缺口）。
5. 侧栏拖宽持久化（记住上次宽度）—— 两端，可选小改进。

### 老仓库有模块但 0 UI 引用（无需迁移，仅记录）
zmodem(rz/sz)、终端背景**图**（背景色已做）—— main/preload/renderer 引用数为 0。

## 9. 相关文档
- `docs/LAYOUT-PARITY.md` — 布局契约
- `docs/BLUEPRINT.md` — 全模块拆分蓝图 + 阶段
- `docs/WIN-CONN.md` — Windows 稳定连接方法（密钥/ACL/pixwin/排查）
- `README.md` — mac 构建运行


## 10. 下次开工检查清单（复制执行）
```bash
# mac：构建 + 跑起来（必须带 DEVELOPER_DIR，CLT 的 SwiftPM 有缺陷）
cd /Volumes/ssd1t/opencode/PixShell-mac
export DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer
swift build && ./.build/debug/PixShell        # 或双击「启动 PixShell.command」

# 出问题先看日志（比截图可靠）
tail -50 ~/Library/Application\ Support/PixShell/logs/pixshell-runtime.log
ls -t ~/Library/Logs/DiagnosticReports/PixShell-*.ips | head -1   # 崩溃栈能直接定位函数

# 本地 CLI 桥自检
TOKEN=$(cat ~/Library/Application\ Support/PixShell/agent_token)
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8766/v1/health            # 期望 401
curl -s -H "Authorization: Bearer $TOKEN" http://127.0.0.1:8766/v1/health            # 期望 200 JSON

# Windows：只能用 pixwin 这个免密别名（密码 SSH 会触发账户锁定）
ssh pixwin "cd /d C:\PixShell-win && dotnet build"
```
**调试钩子**（仅用于复现/截图，不影响正常使用）：
`PIXSHELL_OPEN=sysinfo|tools|backup|conn|cmds|process|network|editor`、`PIXSHELL_EDIT=<远端文件>`、
`PIXSHELL_THEME=light`、`PIXSHELL_AUTOCONNECT=1`、`PIXSHELL_PASS=…`、`PIXSHELL_DEMOCMD=…`、`PIXSHELL_BRIDGE_PORT=…`。
